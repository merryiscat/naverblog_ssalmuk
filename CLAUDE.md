# naverblog_ssalmuk — 네이버 메이트 지원금 파이프라인

네이버 블로그 완전 자동 운영으로 네이버 메이트(월 30만원)에 선정되는 것이 목표.
관찰 → 생산 → 측정 → 매일 자가 보정 루프로 굴러가는 Python 파이프라인.

## 파괴적 자동화 규칙 (2026-08-19 오삭제 사고의 항체 — mistakes.md 참조)

블로그 글 삭제·수정 등 되돌릴 수 없는 조작을 자동화할 때:
1. 파괴적 클릭 **직전에** 대상 식별자(URL의 logNo 등)를 코드로 재검증한다 — 텍스트("삭제")만 보고 클릭 금지
2. dialog 핸들러는 **예상 메시지를 확인한 것만** 수락한다 — 전역 자동 수락 금지
3. 파괴 작업이 "실패한 듯하면 재시도"를 자동화하지 않는다 — 검증 실패는 사람에게 보고 후 중단

## docs 인덱스

프로젝트 관리는 `docs/` 기반 — 전체 목록은 [docs/index.md](docs/index.md).

| 파일 | 내용 | 참조 시점 |
|------|------|----------|
| `docs/plan.md` | 킥오프 요약 — 목표, 메이트 프로그램 구조, 확정 사항, 리스크 | 의사결정·설계 판단 시 |
| `docs/usecases.md` | 유즈케이스 — 공통 전제, 선결 과제, 케이스별 해피/배드/필요기술 | 구현 착수·범위 판단 시 |
| `docs/references.md` | 레퍼런스 그래프 — 유사 구현체·GEO 지식·제재 신호 | 모듈 설계 전 선행 구현체 확인 시 |

## docs/ — LLM 위키 (상시 기록)

docs/는 llmwiki가 관리하는 위키다. 페이지는 LLM이 쓰고 사람은 읽는다.
코드 수정 자체는 git이 기록한다 — 위키는 git이 못 담는 것을 담는다:
**결정과 그 근거, 실수와 그 항체(재발 방지 장치), 채택 안 된 대안, 대화에서 오간 아이디어.**

- **기록은 명령을 기다리지 않는다** — 작업 중 결정·버그·아이디어·방향 전환이 나오면 그 자리에서
  관련 페이지를 갱신하고 `docs/log.md`에 한 줄 남긴다: `## [YYYY-MM-DD] <작업> | <제목>`
- 외부 자료·회의록 파일은 `docs/raw/`에 저장(불변) 후 소화한다. 대화에서 나온 결정은
  페이지에 `derived_from: 대화 YYYY-MM-DD`로 출처를 남긴다
- **버려지는 안건은 시점을 고정해 보류한다** — 갈림길에서 한쪽이 채택되면, 다른 쪽을
  `docs/pending.md`에 재검토 시점(날짜 또는 관찰 가능한 조건)과 함께 기록한다.
  시점 없이 "나중에 검토"로 넘기게 되면, 그 자리에서 사용자에게 시점을 받아 고정한다
- **실수는 연대기에 항체까지** — 실수·비용이 확인되면 `docs/mistakes.md`에 날짜·내용·비용·
  **항체(재발 방지 장치)** 를 1행 추가한다. 처치 설명으로 끝내지 않고, 가드·규칙·린트 같은
  장치를 실제로 만들었는지까지 적는다
- **작업 착수 전 `docs/status.md`(작업 보드)를 먼저 읽는다** — 진행 중·미해결이 거기 있다.
  안 읽으면 이미 문서화된 이슈를 매번 새로 발견하는 것처럼 굴게 된다. 작업이 끝나면
  결과(완료/미해결/재발)를 한 줄이라도 남긴다
- 원본에 있는 주장(asserted)과 종합한 추론(inferred)을 구분해 적는다
- 새 페이지가 생기면 `index.md`에 한 줄 요약을 올린다
- docs 질문은 `index.md` → 관련 페이지 순으로 읽고, 근거가 없으면 없다고 답한다
