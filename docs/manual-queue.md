# 수동 작업 대기열

> 자동화가 못 하는(또는 아직 안 하는) **사람 손이 필요한 작업**을 한곳에 모은다
> (사용자 요청 2026-08-21). 새 항목의 출처: ①검수 오케스트레이터의 manual 분류
> (텔레그램 "🙋 수동 필요" 보고) ②검수 에이전트 지적 중 사람 몫 ③작업 중 발견.
> 보고에 manual이 나오면 이 페이지에 추가하고, 완료되면 취소선 + 날짜를 남긴다.
> ※ 코드로 고칠 수 있는 것은 여기 넣지 않는다 — 그건 [status.md](status.md) 백로그.
> ※ **매주 토 09:00 '대기 중' 표가 텔레그램으로 발송**된다 (스케줄러 manual-queue 잡,
> 사용자 요청 2026-08-21). 서버는 배포 시점의 이 파일을 읽으므로, 항목 갱신 후 배포 필요.

## 대기 중

| 작업 | 방법 | 배경 |
|------|------|------|
| systemd 유닛에 PYTHONUNBUFFERED=1 반영 (선택 — 이중 방어의 한쪽) | 개발 PC에서: `ssh -t merryiscat@192.168.50.205 "sudo sed -i '/Environment=TZ/a Environment=PYTHONUNBUFFERED=1' /etc/systemd/system/naverblog.service && sudo systemctl daemon-reload && sudo systemctl restart naverblog"` | 로그 유실 수정(08-22)의 유닛 쪽 절반 — sudo 비밀번호 필요해 수동. 코드 쪽(line_buffering)만으로 이미 동작 확인됨이라 급하지 않음 |
| 모바일앱 커버 이미지 등록 (선택) | 블로그 관리 → 기본 설정 → 블로그 정보 → 모바일앱 커버 이미지 등록 | 프로필 이미지처럼 구식 팝업 업로더라 자동화 제외. 검수 에이전트가 지적하면 우선순위 상승 |
| **카테고리 구조 재설계** (오케스트레이터 3회 반복 manual: 08-22·25·27) | 관리 → 카테고리 관리에서 10개 메이트 분야명 → 정책 특화 상위 축으로 재편. **코드 의존 있음**(발행 시 분야→카테고리명 매칭) — 이름 바꾸면 매핑 코드 동반 수정 필요. 상세안·단계는 대화 2026-08-30 참조 | "카테고리가 주제어 나열이라 대표 진입점 약함". 완전 정책 특화(08-30)와 함께 재설계 확정 |
| ~~사이드바 위젯~~ → **오탐 해소 (08-31)**: 레이아웃·위젯 설정 실정찰 결과 카테고리·프로필 위젯은 **이미 켜져 있고**, 오케스트레이터가 켜라던 '인기글·대표글 위젯'은 **네이버에 없는 기능**. 위젯은 할 게 없음 | — | 검수 3회 반복 manual이 근거 없는 일반론이었음(inspector.py에 재발방지 가드 추가) |
| **공지글 고정** (운영신호의 실제 레버 — 위 오탐에서 유효분만 남김) | 안내 공지글 발행(서버) + 공지 등록(고정). 문안·최신성 문구 확정됨 | 초방문자·심사자용 안내. 대화 2026-08-31 |

## 완료

| 작업 | 완료일 |
|------|--------|
| ~~기발행 글 5건 각주 원시 텍스트 정리~~ — 글 수정 실행 에이전트(post_editor) 신설로 자동 처리 (5/5 재게시·각주 0건 검증) | 2026-08-22 (post_editor) |
| ~~프로필 이미지 업로드~~ (생성은 자동 — `data/images/profile.png`) | 2026-08-21 (사용자) |
| ~~글 3건 카테고리 이동~~ (여행 → 라이프·라이프·미디어 — 오배정 버그 여파) | 2026-08-21 (사용자) — RSS로 3/3 이동 확인 |
| ~~블로그 카테고리 10개 수동 생성~~ (메이트 10분야명) | 2026-08-19 (사용자) |
