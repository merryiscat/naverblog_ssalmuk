# 실수 연대기

| 날짜 | 실수 | 비용 | 항체 (재발 방지 장치) | 반복? |
|------|------|------|----------------------|-------|
| 2026-08-17 | 위저드 로그인 때 '로그인 상태 유지' 안내 누락 → 저장한 세션이 수 시간 만에 만료, 첫 발행 시도가 세션 거부로 실패 (텔레그램 소환 2건 발생) | 발행 재시도 3회 + 디버깅 | ① 위저드 로그인 안내에 "로그인 상태 유지 체크 필수" 문구 추가 (적용됨) ② 세션 만료는 소환 알림으로 즉시 포착됨 확인 (안전장치 작동) ③ 임시 복구 경로 확보: Playwright MCP 프로필의 살아있는 세션을 storage_state로 내보내기 | |
| 2026-08-17 | 레퍼런스의 asserted 정보("AI 활용 설정 ON")를 실재하는 메뉴로 단정하고 위저드 절차로 만듦 — 사용자가 존재하지 않는 메뉴를 찾아 헤맴. 실제로는 약관 차원 동의 + 공개·검색 허용이 실질 조건이고, 블로그의 'AI 활용' 버튼은 AI 생성물 표시 라벨(동명이인 기능) | 사용자 탐색 시간 + 조사 1회 | asserted 표기가 붙은 항목을 사용자 절차(위저드 단계·체크리스트)로 만들 때는 **절차 문구에 '위치 미검증'을 명시**하고, 가능하면 절차화 전에 실화면·공식 문서로 위치를 검증한다. P8(인용수 UI)은 이미 asserted 표기 유지 중 — 같은 원칙 적용 | |
| 2026-08-19 | 주제 채점(score_llm)이 키워드 **문자열 매칭**이라 LLM이 키워드를 변형 표기하자 전원 0점 — 조용히 골든 점수 폴백되어 브랜드 단독 키워드("엘지전자"·"넷플릭스")가 8/19 selected로 올라감 (채점 기준상 감점 1순위 유형이 최상위 선정) | 재발굴 1회 (발행 전 발견 — 실발행 피해 없음) | ①매칭을 번호(i) 기반으로 교체 ②전멸 시 조용한 폴백 대신 예외 → 텔레그램 알림 ③최종 선정을 오케스트레이터+코드 검증기(분야 중복 교정)로 이중화 — 첫 실행에서 LLM이 여행 2개를 골랐다가 검증기가 교체하는 것 실확인 | |
| 2026-08-19 | **넷플릭스 글 오삭제** — 낡은 글 2건 삭제 자동화에서 3중 실수: ①dialog 전역 자동 수락 ②삭제 후 리다이렉트된 페이지(최신 글 표시)에서 대상 확인 없이 '삭제' 텍스트를 또 클릭 ③삭제 검증이 리다이렉트를 "아직 있음"으로 오판해 디버그 재시도를 유발 → 첫 발행 글(넷플릭스, 게이트 88.2)이 삭제됨. 휴지통 없음 — UI 복구 불가 | 첫 발행 글 URL·이력 소실 (발행 2일차·인용 0이라 실손실 낮음), 복구 재발행 1회 | **CLAUDE.md '파괴적 자동화 규칙' 신설 (2026-08-19)**: ①파괴적 클릭 직전 대상 식별자(URL·logNo) 코드 재검증 ②dialog는 예상 메시지 확인 후에만 수락 — 전역 자동 수락 금지 ③파괴 작업 "실패한 듯하면 재시도" 자동화 금지 — 검증 실패는 보고 후 중단. 복구: 원문이 DB·파일에 남아 있어 제목 정정 후 재발행 예약 (8/19 11:30) | |
| 2026-08-19 | 발행 예약을 스케줄러 메모리(DateTrigger)에만 두는 설계 — 08:30 생성~발행시각 사이 프로세스 재시작이면 예약이 증발해 글이 영영 미발행 (8/18 배포 재시작 때 실제로 발생 가능했던 구조, 사고 전 발견) | 실피해 없음 (구조 결함 단계에서 발견) | **DB 영속 큐 (2026-08-19)**: posts.scheduled_at + 발행 폴러(11~21시 20분 간격) — 재시작해도 예약 유지. 24시간 초과 지연분은 신선도 보호로 자동 취소+알림 | |
| 2026-08-21 | 발행 팝업 카테고리 선택이 **한 번도 실동작한 적 없음** — 휴리스틱 셀렉터("publish 근처 짧은 텍스트 버튼")가 허공을 더듬고 조용히 넘어가, 8/19~20 발행 전 글(3건)이 기본 카테고리 '여행'에 들어감 (소상공인정책자금·희망리턴패키지가 여행에). 8/19 "매칭 10/10 검증" 기록은 카테고리 목록 대조였지 실선택 검증이 아니었음 — 사용자가 화면을 봐서야 발견 | 글 3건 카테고리 오배정 (C-rank 분야 전문성 신호 훼손 — 수동 이동 필요, manual-queue.md) | ①정찰로 실셀렉터 확보(aria-label='카테고리 목록 버튼' + 정확 일치 label 클릭)해 재작성 ②**선택 후 표시값 재조회로 반영 검증** — 미일치면 발행 알림에 ⚠️ 경고 (조용한 실패 금지, publisher.py) ③교훈: "검증"은 실행 경로의 끝값(실제 표시·실제 반영)으로 해야 한다 — UI 목록 대조는 검증이 아니다 ④검수 에이전트가 못 잡은 원인(사용자 질문으로 분석): 체크리스트에 "글이 알맞은 카테고리에 있는가" 항목이 없어 비전이 안 봤고, 값 대조를 코드로 하는 장치도 없었음 → 체크리스트 항목 추가 + **검수 시 코드 대조**(글 상단 카테고리 DOM 읽기 vs DB 분야, 미일치=high 이슈로 합류) 신설 (inspector.py) | |
| 2026-08-22 | **앱 로그 유실 + 게이트 전멸 무통보** — ①systemd 비TTY에서 print가 블록 버퍼에 갇혀 journalctl에 앱 로그가 한 줄도 안 남음(8/20 배포 후 23시간 0줄, "journalctl에 앱 로그 안 남음"을 8/20에 확인하고도 원인 추적 없이 넘어감) ②8/21 생성 4건 전부 게이트 탈락 → 발행 0건인데 텔레그램 무통보 — 탈락 사유는 gate_json에만 묻힘. 사용자가 침묵을 보고서야 로그 검토 요청 | 8/21 발행 0건 + 운영 관찰 불능 상태 ~2일 | ①유닛 `PYTHONUNBUFFERED=1` + scheduler line_buffering 이중 방어 ②posts.skip_reason 컬럼 — 스킵 3지점(리서치 부족·게이트 탈락·예약 초과) 전부 사유 기록 ③생성 사이클 종료 시 텔레그램 요약 1건, **합격<목표(2건)면 소환** — 침묵과 정상을 구분 가능하게 ④교훈: "로그가 안 남는다"는 관찰은 그 자체가 P1 버그다 — 기록만 하고 넘기지 않는다 | |
| 2026-08-18 | 게이트에 신선도 차원이 없어 낡은 리서치 스니펫 기반 글이 통과 — "2024년 여름 기준 강릉", "2024년 6월 기준 속초" 2건이 2026년 8월에 실발행됨 (사용자가 화면을 직접 봐서야 발견 — 시스템 자가 감지 실패) | 낡은 글 2건 공개 발행 (블로그 신뢰도·AI 브리핑 인용 불리), 발행 슬롯 2개 소모 | **왕복 루프 v1 (구현·배포 완료 2026-08-18)**: ①소스별 나이 주석 + 1년+ "⚠낡음" 표시 ②게이트 freshness 차원 — 단독 커트라인 60 (사실성과 동급) ③re_research 액션 — 소스가 낡으면 재리서치로 소스 교체 ④작문 프롬프트에 오늘 날짜 주입·과거 연도 기준 금지. 검증: 같은 유형(속초) freshness 41로 차단 확인. 자가 감지 공백은 3순위 블로그 검수 에이전트(화·목·토)가 추가로 막을 예정 | |
